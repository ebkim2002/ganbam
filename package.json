{
  "name": "ganbam-security-tests",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-ganbam-security \"node --test tests/firestore.rules.test.cjs\"",
    "test:app": "node --test tests/app.test.cjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.18.0",
    "firebase-tools": "15.29.0",
    "jsdom": "30.0.1"
  }
}
